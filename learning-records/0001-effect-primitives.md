# Effect Primitives

User knows Effect's core primitives: the `Effect<A, E, R>` type, `pipe`, `Effect.gen` generators, `yield*` for dependency/service extraction. Comfortable creating and composing effects. This is the foundation Layer builds on — the `R` parameter is where Layer injects services.

Evidence: User stated "good understanding of the Effect primitives."
